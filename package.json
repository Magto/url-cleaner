{
  "name": "url-cleaner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "update-rules": "node scripts/update-rules.mjs"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
